---
name: ref2pptx
version: "2026-07-16.03"
description: |
  把一個「專案資料夾」裡的參考資料轉成一份 pptx 投影片,並以「受眾人物誌審查」作為品質關卡。
  專案資料夾用編號子資料夾分區:00_Masters(投影片母片 / 範本)、10_Input(資料暫存區,新素材先放這裡)、20_References(實際採用來做投影片的資料)、30_Output(產出的投影片與報告)、40_Scripts(最近一輪的產生腳本)、90_Excluded(評估過、不適合使用的資料 + 封存的舊版產出與腳本)。

  skill 會先把 10_Input 的素材篩選成「採用(移到 20_References)」或「排除(移到 90_Excluded 並記原因)」,再用 20_References 做簡報。
  每次產出**一份**簡報,由 PM、QA、RD 加上 USER-zen(佛系終端用戶)、USER-picky(找碴終端用戶)組成的**完整小組**全員審查通過。
  每章至少三個例子、涵蓋三方視角。
  **每個主題都必備一個「三方視角」欄位**(主題跨多頁時放在最後一頁、續頁標題加 (cont'd)):三行分別以 [PM]、[QA]、[RD] 標籤寫出各角色對該主題的角度 / 例子,再加一句「簡單來說」白話總結。

  內容主來源是根目錄 OUTLINE.md(章節結構+各章內容);
  20_References 是補充素材;
  素材不足時可上網查,但用到的網路內容要在該頁留下來源網址。

  每次執行會先檢查 OUTLINE.md 與 SKILL 版本是否變更,也會比對 20_References / 90_Excluded 裡資料夾內容有沒有被擴充。
  OUTLINE 很長(預估超過約 40 頁)就走分批模式:建 PROGRESS.md 進度帳、一次只做一章、分章檔最後合併、交付前對帳確認沒漏章。

  產出前一定要問使用者:00_Masters 有母片時要不要套 / 套哪一個,以及長寬比(16:9、4:3 或其他 PowerPoint 支援的比例);
  若 30_Output 已有上一版簡報(使用者可能已手動修改),還要問是要以它為基礎延伸、還是從參考資料重新產生。

  另有可選的產出後外部審查迴圈:50_Reviewer 放其他 AI 的審查意見,先把意見分四類——客觀錯誤自動修,主觀取捨 / 大改 / 動到既有功能或規則的一律先提計畫、經使用者核准再動(AI 不擅自大改或掏空教材);修訂成新版 vXX(與 generate 腳本同號),分類理由記進 51_Revised/vXX_YYYYMMDD 的 LOG,意見不得凌駕硬規則。
  產出固定含:封面、目錄頁(自動編號)、簡報脈絡心智圖頁、各章內容、參考資料章節(原文照登、免審查);OUTLINE.md 標記圖檔檔名就產一頁專放那張圖;每頁備忘稿記講者備忘與本頁資料來源。
  為安全起見產出一律是無巨集的 .pptx,並做法律風險檢查(不得出現性騷擾、歧視、職場霸凌、誹謗、隱私洩漏等可能讓講者被告的內容),並在每張內容頁底部放進度條 / 小大綱。

  素材命名含 `(O)` 記號者優先採用;
  採用的資料夾若含 `URL.txt`(檔名不分大小寫),把該網址當來源,標在用到它的投影片進度條上方(字小即可)。

  當使用者要把一批資料(文件、筆記、規格、研究)整理成簡報,尤其是要給非技術或特定背景受眾看的教育訓練 / 內訓 / 課程簡報時,使用這個 skill。
  只要使用者提到「參考資料 → 投影片 / PPTX」「把這些資料做成簡報」「給非技術人員的課程」,即使沒明講受眾審查,也用這個 skill。
---

# ref2pptx — References → PPTX(參考資料轉投影片產生器)

把一個**專案資料夾**裡的參考資料,轉成一份**對指定受眾真正聽得懂**的 pptx 投影片,並把產出寫回專案資料夾的 `30_Output/`。

這個 skill 的價值不在於「會做投影片」(實際排版交給 `pptx` skill),而在兩件事:一是**先把素材篩過一輪**(採用 / 排除分流,讓做簡報的資料是乾淨的);二是**強制每個章節都通過受眾人物誌審查**——用一位真實背景的受眾的眼睛重讀章節,確認沒有他看不懂的術語、且有足夠且貼近他日常工作的例子。這是為了避免簡報變成「作者覺得清楚、受眾一臉問號」。

---

## 專案資料夾結構

這個 skill 以**一個專案資料夾**為單位運作。使用者會提供專案資料夾的路徑,裡面用**編號子資料夾**分區:

```
<專案資料夾>/
├── INDEX.md         專案總覽地圖(skill 自動維護,給人快速掌握全局)
├── README.md        專案說明:這是什麼、怎麼用 / 重跑(skill 自動維護,給人看)
├── CLAUDE.md        專案記憶:給 Claude Code 的慣例與現況(skill 自動維護,給 AI 看)
├── AUDIENCE.md      受眾設定(控制檔,放專案根目錄)
├── OUTLINE.md       章節結構 + 各章內容(投影片主來源,放專案根目錄)
├── GLOSSARY.md      中英詞彙對照表(控制檔,選用;統一全簡報用語)
├── 00_Masters/      投影片母片 / 範本(.pptx / .potx,選用)
├── 10_Input/        資料暫存區:新素材先丟這裡,待篩選
├── 20_References/   補充參考素材(圖、要引用的文件等;skill 會一併取用)
├── 30_Output/       產出:簡報 .pptx + review-report.md
├── 40_Scripts/      產生腳本(最近一輪;可重跑 / 微調再產)
├── 50_Reviewer/     其他 AI 產出後的審查意見(待處理;處理完清空)
├── 51_Revised/      據外部審查修訂的歸檔:每版一個子資料夾 vXX_YYYYMMDD(內含用掉的意見 + LOG)
└── 90_Excluded/     評估過、不適合使用的資料 + 封存的舊版產出與舊腳本
```

`AUDIENCE.md`、`OUTLINE.md`、`GLOSSARY.md` 是**控制檔,放在專案根目錄**(不放進 `20_References/`)。其中 **`OUTLINE.md` 同時是章節結構與各章內容,是投影片的主來源**——調章節順序、改內容都在這個頂層檔完成,不必進子資料夾。`GLOSSARY.md`(選用)是**中英詞彙對照表,統一整份簡報的用語**(見「詞彙對照表」)。控制檔不參與篩選、永遠不會被移到 `90_Excluded/`。

**主來源 vs 補充素材**:投影片骨架與內容以根目錄 `OUTLINE.md` 為主;`20_References/` 是**補充參考素材**(圖、要引用的文件、數據來源等),skill 會取用來豐富 / 佐證各章,但不是骨架。兩者衝突時以 `OUTLINE.md` 為準。章節順序一律以 `OUTLINE.md` 的標題順序為準,skill 不自行重排。

`INDEX.md`、`README.md`、`CLAUDE.md` 是 **skill 在每次跑完自動寫 / 刷新的根目錄文件**(見第 10 步),三者用途不同、不要重複:`INDEX.md` 是當前狀態地圖、`README.md` 給人看怎麼用、`CLAUDE.md` 給 Claude Code 記住專案慣例與現況。它們都不是素材。

開工前先用 `view` 列出專案資料夾,確認各區狀態:

- `20_References/` 和 `10_Input/` **兩個都空 / 都不存在**:沒有任何素材可用,停下來問使用者素材放哪,不要自己亂猜。
- `30_Output/` 不存在:直接建立一個再往下。
- `00_Masters/`、`90_Excluded/` 不存在:需要時自動建立即可(`90_Excluded/` 在第一次排除素材時建)。

子資料夾名稱以上述編號為預設;若使用者的專案用別的名字,依使用者實際命名為準,但**角色對應不變**(母片 / 暫存 / 採用 / 產出 / 排除)。

### 00_Masters — 投影片母片

放 PowerPoint 範本檔(`.pptx` / `.potx`)。**選用**。處理規則見下方「開工前必問 → 母片」:只要裡面有檔案就**必須先問使用者**,不自作主張。

### 10_Input — 資料暫存區

使用者把**新蒐集、還沒篩過**的素材先丟這裡(可以是單檔,也可以是**整個資料夾**)。skill 會在第 1 步把這裡的東西逐件評估,分流到 `20_References/`(採用)或 `90_Excluded/`(不適用);**資料夾以整個為單位搬移**(見第 1 步)。處理完後 `10_Input/` 理想上會被清空(東西都分流走了)。

### 20_References — 補充參考素材

放**補充用的參考素材**,用來豐富 / 佐證各章內容(投影片骨架與主內容來自根目錄 `OUTLINE.md`)。可以是使用者直接放好的、也可以是從 `10_Input/` 篩選後移進來的。可有可無——只有 `OUTLINE.md` 也能產出簡報。

| 內容 | 用途 | 是否必要 |
|------|------|----------|
| 圖、截圖、流程圖 | 放進對應章節的視覺 | 選用 |
| 要引用的文件 / 數據來源(md / docx / pdf / txt) | 佐證、補充細節 | 選用 |
| 含內容的資料夾(可含 `URL.txt`、`(O)` 記號) | 成組的素材 + 來源網址 | 選用 |
| 品牌 / 風格說明、Logo、配色 | 套用視覺風格 | 選用 |

> 主來源是根目錄 `OUTLINE.md`;`20_References/` 是補充。兩者衝突以 `OUTLINE.md` 為準。設定檔 `AUDIENCE.md`、`OUTLINE.md` 放根目錄,不放這裡。

讀檔前先用 `file-reading` skill 判斷各檔該怎麼讀(尤其 pdf / docx / 圖,不要直接 `cat` 二進位檔)。

### 30_Output — 產出

所有產出都寫進這裡,**不要寫到別區,也不要改動 `20_References/`、`00_Masters/`**(`40_Scripts/` 放腳本、`51_Revised/` 放修訂歸檔與 LOG 屬例外;`50_Reviewer/` 讀完把意見移走;`90_Excluded/` 只做「移入封存 / 移出復用」,不直接編輯裡面的檔):

- `<簡報名>.pptx` —— 最終簡報(完整小組審過),**無巨集 `.pptx`**(見「安全:禁止巨集」)。
- `PROGRESS.md` —— (長大綱分批模式)章節進度帳:每章一列、狀態(待做 / 已產 / 已審 / 已 QA),見「長大綱(分批)模式」。
- `review-report.md` —— 逐章記錄完整小組各審查者第幾輪通過、用了哪些例子;以及素材篩選結果摘要。這份報告就是驗收證據。

**產生腳本不留在這裡**:製作過程寫的 `generate_*.py` / `slides_*.py` 等腳本,產出完成後一律歸到 `40_Scripts/`(見下),讓 `30_Output/` 只放「做出來的東西」,不混「怎麼做出來的」。

### 40_Scripts — 產生腳本(最近一輪)

放**這一輪**用來產生簡報的腳本(例如 `generate_v03.py`、`slides_v03_ch2.py`),讓簡報能**重跑、微調再產**,不必從頭重寫:

- **只留最近一輪**:換版重產時,把上一輪的腳本移走(見下),這裡永遠只有目前這份簡報對應的腳本。
- **與產出對得起來**:腳本的版本標記要和 `30_Output/` 的簡報對應(同一輪)。
- **不放素材、不放產出**:這裡只放程式;圖檔等素材仍在 `20_References/`,簡報在 `30_Output/`。

### 50_Reviewer — 其他 AI 的產出後審查意見(待處理)

放**其他 AI 看完投影片後寫的審查意見**(產出**之後**的外部回饋,和產出前的內部完整小組並存、不衝突)。格式不拘(.md / .txt / .docx…、一頁一則或整份一則);skill 逐檔 / 逐則讀,能對應到哪一頁就標註哪一頁。

- **這是可選、獨立的一步**:這裡是空的就跳過(不是每次都要有外部意見)。
- **一次一批**:裡面可能有多個 AI、多個檔的意見,**當作一批一起處理成同一個新版**(不要一檔一版,避免版本爆炸)。
- 處理流程見第 8.5 步「據外部審查修訂」;處理完這裡會被清空(意見移到 `51_Revised/`)。

### 51_Revised — 據外部審查修訂的歸檔

每次據 `50_Reviewer/` 的意見修訂出一個新版簡報,就在這裡建一個子資料夾 **`vXX_YYYYMMDD`**(`vXX` = 該版簡報版本號、與 `40_Scripts/generate_vXX.py`、`30_Output/<簡報名>_vXX.pptx` **同號**;`YYYYMMDD` = 修訂日期),裡面放:

- **這次用掉的那批審查意見**(從 `50_Reviewer/` 移過來,不是複製)。
- **`LOG.md`**(每版一份,跟該批意見綁在一起):記這版**改了什麼、沒改什麼**——見第 8.5 步。
- 投影片本體**不放這裡**(仍在 `30_Output/`,避免到處複製大檔);LOG 註明對應的 `30_Output/` 版本檔名。

### 90_Excluded — 排除的資料 + 封存的舊版產出

這個資料夾有兩種用途,在 `90_Excluded/EXCLUDED.md` 裡**分開兩區**記錄,別混在一起:

- **排除的素材**:評估後**不適合用**的素材移來這裡(不要刪除,保留讓使用者覆核),逐筆記一行原因(例如:離題、重複、解析度太低看不清、過期版本)。
- **封存的舊版產出**:重跑時把 `30_Output/` 既有的舊版簡報**連同 `40_Scripts/` 裡產生它的那份腳本**,一起加時間戳移來這裡保存(見第 8 步)——**簡報與腳本成組封存**,日後要回溯「上一版那頁是怎麼產的」才找得到。這些是過去版本、不是不適合的素材,要標明清楚。**一律用移動、不刪除**。

---

## 開工前必問(最多三個一定要問的設定)

在開始產出投影片**之前**,以下事項一定要問使用者,不要自己預設:

### 1. 母片(若 `00_Masters/` 有檔案)

- **空的 / 不存在**:不套母片,從零做(走 `pptx` skill 的 `pptxgenjs.md`)。
- **剛好一個檔**:問「要套用這個母片嗎?(檔名:`<x>`)」。要 → 套用(走 `pptx` skill 的 `editing.md`);不要 → 從零做。
- **超過一個檔**:列出所有檔名,問「要套用哪一個母片?」(並提供「都不套」)。選定後才往下。

套母片只決定外觀,不影響「素材篩選 + 每章受眾審查 + 3 個例子」這些品質關卡。

> **一律用「投影片母片」(Slide Master),不要用「備忘稿母片」或「講義母片」。** PowerPoint 有三種母片,作用不同:**投影片母片(Slide Master)+ 版面配置(Slide Layouts)** 決定投影片本身的外觀與字級,這才是要套的;**備忘稿母片(Notes Master)** 只管「備忘稿列印頁」的樣子,**講義母片(Handout Master)** 只管講義列印——這兩個都**不可**拿來決定投影片外觀 / 字級。在 `.pptx` 結構裡,要參照的是 `ppt/slideMasters/` 與 `ppt/slideLayouts/`,**不是** `ppt/notesMasters/`、`ppt/handoutMasters/`。下一節「字級完全照母片」指的字級,也一律以**投影片母片 / 版面配置的佔位字級**為準。

### 2. 長寬比

一定要問使用者要用哪種長寬比,選項至少包含:

- **16:9 寬螢幕**(13.333 × 7.5 吋)—— 現代投影 / 螢幕最常用。
- **4:3 標準**(10 × 7.5 吋)—— 舊投影機 / 部分企業範本。
- **其他 PowerPoint 支援的比例**:例如 16:10、A4、Letter、橫式 / 直式,或使用者自訂的吋 / 公分尺寸。

若使用者**同時選了母片**,而母片本身的尺寸與所選長寬比不同,要回頭確認「以母片尺寸為準,還是覆蓋成所選的長寬比?」——預設尊重母片,除非使用者明確要覆蓋。

### 3. 沿用 / 延伸上一版(僅當 `30_Output/` 已有簡報時才問)

**重跑時**,如果 `30_Output/` 已經有上一版簡報,**一定要先問**使用者要走哪一種——因為使用者可能已經**手動修改過**那份簡報,那些修改不該被無聲蓋掉:

- **(A) 以上一版為基礎延伸**:把 `30_Output/` 既有的簡報當成基礎(它可能含使用者的手動修改),只在其上套用這次要的變更——走 `pptx` skill 的 `editing.md` 編輯既有檔,**保留既有內容與手動編輯**。此時上一版簡報等同「參考底稿」。
- **(B) 從 `20_References/` 重新產生**:不沿用手動修改,照標準流程從參考資料重建。

無論選哪一種,動手前都**先把既有簡報加時間戳封存到 `90_Excluded/`**(見第 8 步),手動修改的版本不會遺失。若使用者選 (A) 但 `20_References/` 也有新素材,延伸時一併納入。


---

## 安全:禁止巨集

為安全起見,**產出的簡報禁止啟用巨集**:

- 輸出一律存成 `.pptx`,**絕不**輸出 `.pptm` / `.potm` 等啟用巨集的格式。
- 即使 `00_Masters/` 的母片是 `.pptm` / `.potm` 或內含 VBA / 巨集,**都不可把巨集帶進產出** —— 套用其外觀,但另存為乾淨的 `.pptx` 並去除任何 VBA / 巨集(打包後不應有 `ppt/vbaProject.bin`)。
- 若母片明顯依賴巨集才能運作,告知使用者該母片含巨集、產出會以無巨集版本呈現。

---

## 工作流程

整體順序:**檢查 OUTLINE.md / SKILL 版本是否變更 → 整理素材(暫存→採用/排除)→ 消化內容(OUTLINE.md+補充素材,不足可上網查)→ 定審查小組(完整小組)→ 開工前必問(母片 + 長寬比 + 若有舊版問沿用/重產)→ 排章節 → 逐章寫(含 3+ 例子 + 三方視角欄位)→ 逐章審查(完整小組全員)→ 修到全員通過 → 產 pptx(封面 + 目錄 + 心智圖 + 無巨集 + 進度條 + 來源網址)→(若 50_Reviewer 有外部意見)據意見修訂成新 vXX + 記 LOG + 歸檔 51_Revised → QA → 更新 INDEX.md / README.md / CLAUDE.md(含 OUTLINE 指紋 + SKILL 版本)**。素材篩選與審查是兩道擋在產出前的關卡。

### 0. 檢查 OUTLINE.md 與 SKILL 版本是否變更(每次執行都做)

`OUTLINE.md` 是內容主來源,所以每次跑**先比對它有沒有變**:

- 算 `OUTLINE.md` 目前的指紋(對內容做雜湊,例如 sha256 取前 12 碼),跟**上次記在 `INDEX.md` 的指紋**比;若 `30_Output/OUTLINE.used.md`(上次產出所用的快照)存在,也可直接 diff 看**哪幾章變了**。
- **首次 / 沒有舊指紋**:當作新專案,照常往下。
- **有變更**:告訴使用者變了(有快照就指出哪些章節新增 / 刪除 / 修改);這些章節要重做。這也接到「開工前必問」的沿用/重產判斷。
- **未變更**:告訴使用者 `OUTLINE.md` 自上次產出後沒變。
- **其他控制檔變更**:`AUDIENCE.md`、`GLOSSARY.md` 若有更動(可比對 `INDEX.md` 記的指紋),對應要重做——受眾變→重審;詞彙表變→回頭校對用語一致。若 `30_Output/` 已有簡報、且 `20_References/`、`10_Input/` 也沒新素材,就提醒使用者「內容來源沒變,確定要重跑嗎?」讓他決定(可能只是想換母片或長寬比)。

**同時比對 SKILL 版本**:讀本 skill frontmatter 的 `version`(日期式,格式 `YYYY-MM-DD.NN`,如 `2026-07-16.01`),跟**上次記在 `INDEX.md` 的 SKILL 版本**比:
- 不同(或 INDEX 沒記過)→ 告訴使用者「**ref2pptx 規則自上次產出後更新過(舊版 → 新版)**,這份簡報是用舊版規則做的,建議重跑以套用新規則」。
- 相同 → 不用特別說。
- 維護慣例:**每次改動 SKILL.md,就把 `version` 更新成「當天實際日期 + 當日流水號」,格式固定為 `YYYY-MM-DD.NN`(流水號固定兩位數)**——**每天的第一個版本一律從 `.01` 開始**(例如 `2026-07-16.01`),同一天每改一次就 +1(`.02`、`.03`…);換天就換日期、流水號歸 `.01`。**重點:日期要跟著真實日期走,不要沿用上次的舊日期**。兩位數流水號讓版本字串**可直接依字典序排序**。這個欄位一定讀得到(在 frontmatter、隨 skill 載入),所以跨環境都能比對,不必去磁碟找檔。
- (進階 / 選用:若能定位到 skill 安裝路徑的 `SKILL.md`,可改對該檔做 sha256 來自動偵測任何改動;定位不到就以 `version` 欄位為準。)收尾(第 10 步)時把目前 `version` 一併寫進 `INDEX.md`。
- **`TECH_NOTES.md` 也帶同一個版號**(檔頭):改動任一檔時,**兩檔版號一起更新**成同一個 `YYYY-MM-DD.NN`,保持同步。

### 1. 整理素材(暫存 → 採用 / 排除)

把素材篩成乾淨的一組再開始做簡報:

- **把 `10_Input/` 的每一件都當成「第一次看到」**:使用者可能把先前排除、現在想重評的東西**手動從 `90_Excluded/` 移回 `10_Input/``**——因為簡報內容會不斷更新,以前不適用的,現在可能用得上了。所以一律依**現在的主題與簡報狀態**重新判斷,**不要**因為它「以前被排除過」就再排除,也**不要**去翻 `90_Excluded/EXCLUDED.md` 的舊判斷來預設立場。判斷相關性時對照的是當下的內容(若這次是「延伸」上一版,就連同既有簡報一起對照)。
- **OUTLINE 有新增章節時,才主動回頭挖 `90_Excluded/`**:若第 0 步偵測到 `OUTLINE.md` **新增了章節**,就掃一遍 `90_Excluded/` 裡「排除的素材」,對照新章節與主題,看有沒有**之前棄用、這次用得上**的(例如那時離題、現在新章節剛好需要)。判斷「當第一次看到」。(沒有新增章節就不必每次掃。使用者仍可隨時手動把東西移回 `10_Input/`。)
  - 現在可用 → 移回 `20_References/`(資料夾比照規則補 `NN_` 索引),並從 `EXCLUDED.md` 的「排除的素材」區移除該筆。
  - 仍不適用 → 留在原處,不必動。
  - **只看「排除的素材」,不要動「封存的舊版產出」**(那些是歷史簡報,不是素材)。
  - 在 `review-report.md` 記一筆:這次從 `90_Excluded/` 復用了哪些、為什麼現在可用。
- 逐件看 `10_Input/`(必篩)與 `20_References/` 裡的素材。讀檔前先用 `file-reading` skill 判斷怎麼讀。
- **`10_Input/` 裡若是資料夾,整個資料夾當一個單位處理**:把它當成一組一起評估,然後**整個資料夾**移到 `20_References/`(採用)或 `90_Excluded/`(排除),不要拆開裡面的個別檔案分開搬。
- **清掉 `20_References/` 裡用不到的素材**:做完內容消化 / 產出後,`20_References/` 裡若有**這次完全沒被任何章節採用**的素材,就把它**移到 `90_Excluded/`**(用移動、不要刪除),並在 `EXCLUDED.md` 的「排除的素材」記一筆原因(例如「本次未採用」)。**若該檔在某個資料夾內,整個資料夾一起移**(跟採用時「資料夾整包移動」一致),不要只挑單檔搬。之後若又用得上,會在 OUTLINE 新增章節時、或該資料夾被擴充時從 `90_Excluded/` 撈回(見上 / 見下)。這讓 `20_References/` 只留「這份簡報實際有用到」的素材、保持乾淨。
- **檢查 `20_References/` 與 `90_Excluded/` 裡資料夾的內容有沒有變(可能後來又擴充了資料)**:這兩個區的資料夾不是一成不變——使用者可能事後往某個既有資料夾**再丟新檔案 / 擴充內容**。所以每次執行都比對各資料夾**目前的內容**(檔案清單 + 大小 / 修改時間,或內容指紋)跟**上次記在 `INDEX.md` 的**有沒有不同:
  - `20_References/` 裡某資料夾**多了 / 改了檔案** → 那組素材被擴充了,要**重讀並把新內容納入**對應章節(不是只沿用上次讀過的)。
  - `90_Excluded/` 裡某「排除的素材」資料夾**多了 / 改了檔案** → 它可能因為補了資料而**變得可用了**,要**重新評估**;可用就比照規則移回 `20_References/`。(「封存的舊版產出」不在此列,不重評。)
  - 在 `INDEX.md` 記下這兩區各資料夾目前的內容清單 / 指紋,供下次比對。
- **判斷適不適用**:跟主題相關嗎?品質 / 可讀性夠嗎(圖太糊、字太小看不清?)?跟別的素材重複嗎?是不是過期版本?
- **採用**:適用的素材留在 / 移到 `20_References/`。
  - **採用的若是資料夾,移進 `20_References/` 時要在原資料夾名稱前加數字索引前綴**(格式 `NN_原資料夾名`,兩位數補零、底線分隔,例如 `白板筆記` → `03_白板筆記`),這樣 `20_References/` 內的排序才不會亂掉。
  - **索引怎麼取**:看 `20_References/` 現有帶 `NN_` 前綴的項目(檔案與資料夾一起算),取最大號 +1;沒有就從 `01` 起。一次採用多個資料夾就依序給連號(`03`、`04`…)。已經有 `NN_` 前綴的資料夾不要重複加。
- **排除**:不適用的**移到 `90_Excluded/`**(用 `mv`,**不要刪除**);若是資料夾就整個移過去。並在 `90_Excluded/EXCLUDED.md` 加一行寫清楚名稱與排除原因(排除區不需要加索引前綴)。`EXCLUDED.md` 以「現在 `90_Excluded/` 裡實際有什麼」為準:同一項再次被排除就更新該行(不要堆疊重複),已被使用者移回 `10_Input/` 而不在 `90_Excluded/` 的舊項可清掉。
- `20_References/` 是使用者刻意放的「要用」的素材:除非明顯無法使用(壞檔、完全離題),否則優先採用;若要把 `20_References/` 裡的東西排除,**先告知使用者**再移。
- **`(O)` 優先記號**:名稱裡含 `(O)` 的檔案或資料夾,是使用者標記的**優先素材**——一律採用,並標為高優先來源;之後寫章節、選素材、取例子時**優先使用**這些。搬進 `20_References/` 時保留 `(O)`(若同時要加索引前綴,放在前綴之後,例如 `03_白板筆記(O)`)。
- **`URL.txt` 來源網址**:採用的**資料夾裡若有 `URL.txt`**,代表那是這份素材的**來源網址**。**檔名不分大小寫**:`url.txt`、`URL.txt`、`Url.txt`、`URL.TXT` 等都算數(比對時一律轉小寫再比)。讀出裡面的網址,記住「這個資料夾 → 這個網址」的對應;用到這份素材內容的投影片,要在頁面標上來源(見第 8 步進度條設計)。`URL.txt` 本身是中繼資料,不當成簡報內容、也不列入排除。
- 篩完把結果摘要(採用幾件 / 幾個資料夾、各自新索引、`(O)` 優先項、有來源網址的資料夾、排除幾件與原因)記進 `30_Output/review-report.md`。

### 2. 消化內容(主來源 OUTLINE.md + 補充素材)

先讀**根目錄 `OUTLINE.md`**——它是投影片的章節結構與各章內容的主來源。再讀 `20_References/` 的**補充素材**(圖、引用文件、數據來源),整理出每章可用的真實案例 / 數據 / 圖。標記哪些概念**天生帶技術門檻**(之後審查要特別盯)。內容衝突時**以 `OUTLINE.md` 為準**,`20_References/` 用來豐富 / 佐證;補充素材若有 `(O)` 優先記號,在補充取捨時優先採用。同時建立「哪段內容用到哪個素材、那個素材 / 資料夾有沒有 `URL.txt` 來源」的對應,供產出時標來源用。

**素材不足時可上網查(要留網址)**:若 `OUTLINE.md` + `20_References/` 不足以支撐某章(缺定義、缺實例、需要較新的事實或數據),**可以上網搜尋來補**。但有兩個硬規定:
- **凡是用到網路查來的內容,該頁一定要留下來源網址**(做法同 `URL.txt`,標在進度條上方,見第 8 步)。沒留網址就不能用網路內容。
- 用可信來源(官方文件、原始出處優先),**不要照抄整段**——用自己的話改寫、只取重點;不確定就標示存疑或回報使用者,不要編造。
- 仍以使用者自己的素材優先,網路只是補洞;補了什麼、用了哪些網址,記進 `review-report.md` 與 `INDEX.md`。

### 3. 確定受眾人物誌

先讀**專案根目錄的 `AUDIENCE.md`**(不在 `20_References/`)。沒有就用下方預設庫,並向使用者確認。每位受眾至少要寫清楚三件事,審查時才有判準:

- **背景**:他會什麼、不會什麼(這是審查的核心)
- **日常工作**:他每天在處理什麼,例子要貼著這個給
- **看不懂就會卡住的東西**:哪類術語 / 假設前提對他是黑箱

**聽眾學歷基準(全體適用,凌駕個別人物誌的『不懂』判斷)**:假設**所有聽眾都大學畢業**——一般知識、閱讀理解、邏輯推理都是大學程度。由此:
- **不要過度簡化 / 太一般化**:不用幼稚的比喻、不把常識當新知從頭解釋、不把聽眾當外行小白。寫給「受過高等教育、只是不熟這個領域」的成年人看。
- **技術字眼以「高中就學過的」為基準**:高中(含)以下程度的科學 / 數學 / 資訊名詞(例如機率、函數、變數、流程圖、資料庫概念這類)**可直接用、不必解釋**;**只有超過高中程度的專業術語才需要解釋**(或用「縮寫角落註」帶出全名)。
- 各人物誌(PM / QA / RD / 終端用戶)列的「不會 / 看不懂」是**在這條底線之上**再加各自的專業限制——例如 PM 不懂程式架構、QA 不會寫 code,但他們都看得懂高中程度的名詞、不需要被當小學生講解。

預設受眾庫(可直接用、可改、可加):

| 代號 | 受眾 | 會 / 不會 | 例子要貼近 |
|------|------|-----------|-----------|
| `PM-nontech` | 完全沒有技術背景的 PM | 懂產品與流程,不懂程式 / 架構 / 指令;可接受英文技術縮寫(每張投影片角落都附全名) | 需求、排期、溝通、驗收 |
| `PM-tech` | 技術轉型 PM(原本是 RD 或 QA) | 有技術底、看得懂術語與技術內容;工作是把外界來的資訊消化後,用**通識語言**講給其他人聽、或跟**客戶討論規格**——很在意「這段能不能轉述成讓非技術的人 / 客戶也聽得懂」 | 把技術內容翻成白話轉述、跟客戶談規格 / 釐清需求、技術 ↔ 商業之間的橋接 |
| `QA-nocode` | 不會寫 code 的 QA | 懂測試設計與情境,不會自己寫程式 | 測試案例、缺陷重現、驗收條件 |
| `QA-nolinux` | 不熟 Linux 的 QA | 會測試,但對指令列 / 伺服器環境陌生 | 用 GUI / 圖形工具完成同一件事 |
| `QA-guionly` | 只會測 GUI、無法揣摩使用者的 QA | 會照案例點 GUI 驗收,不碰底層;難以自行推想使用者意圖 | 「使用者想做什麼→怎麼點→看到什麼」寫到具體,情境寫明不靠腦補 |
| `RD-spec2code` | 只會把 spec 翻成 code 的 RD | 能照規格實作,較少做架構 / 取捨判斷 | 規格如何對應到實作、邊界條件 |
| `USER-zen` | 佛系終端用戶 | 什麼都不懂、只負責用;遇到 bug 自然繞過,不發問、不抱怨 | 直接照做就會的步驟;不能假設他會主動動腦 |
| `USER-picky` | 找碴終端用戶 | 什麼都不懂;看不懂 / 不會用就直接說「東西爛」 | 清楚到挑不出毛病、零含糊零跳步的內容 |

**審查小組(完整小組,全員都要過)**:每章都由**完整小組**審查,**全員通過才算過**。成員 = **PM / QA / RD + USER-zen + USER-picky**。各成員用哪個人物誌在 `AUDIENCE.md` 指定,預設:PM = `PM-nontech`、QA = `QA-nocode`、RD = `RD-spec2code`、USER-zen(佛系)、USER-picky(找碴)。在 `30_Output/review-report.md` 記下成員與通過情形。

各成員的通過條件:
- **PM / QA / RD**:對該角色沒有未解釋的術語 / 黑箱假設;找得到至少一個貼近他、看得懂的例子;能從頭跟到尾不卡。
- **`USER-zen`(佛系)**:他不會發問,看不懂就默默掉隊;通過 = 被動讀也能跟到底,沒有需要自行推敲的斷點。
- **`USER-picky`(找碴)**:看不懂就一直抱怨;通過 = 找不到明顯可被他挑成「看不懂 / 不會用」的地方。

### 4. 排出章節大綱

**章節結構與順序一律以根目錄 `OUTLINE.md` 的標題順序為準**,不要自行重排或增刪章節。`OUTLINE.md` 各章標題下的內容就是該章主體;`20_References/` 的補充素材歸到對應章節。每章之後都交完整小組審查。(若使用者沒有 `OUTLINE.md`,才依素材自行擬章節並先跟使用者確認。)

### 5. 逐章撰寫(內含 3+ 例子規則)

每章撰寫時就**內建**驗收標準 3.B:

> 每個章節必須有**三個以上**例子,且要**涵蓋 PM / QA / RD 三方視角**——三方各自都要找得到至少一個對他有意義的例子(三個剛好可一方一個,需要時更多)。

例子要具體到該角色能對號入座(用他工作中真的會碰到的情境、名詞、畫面),不能是抽象換句話說。例子盡量涵蓋不同面向(例如:正常情況 / 例外情況 / 對比錯誤做法),而不是同一個例子講三次。**寫例子時就避開法律風險**:不用刻板印象、不點名真實同事 / 主管 / 競品當負面例子、不放性暗示或歧視 / 霸凌字眼(詳見第 9 步「法律風險 QA」)。

**內文用語:儘量不用「你」**:投影片內文(含標題、條列、三方視角、簡單來說等)儘量**不要用第二人稱「你」**稱呼受眾。省略主詞而語意不變時就**省略**;一定要有主詞時改用「**我們**」。例如「你可以先問客戶最怕什麼壞掉」→「可以先問客戶最怕什麼壞掉」或「我們可以先問客戶最怕什麼壞掉」。例外:**參考資料章節與任何引用原文照原樣呈現**(原文照登,不改字)。

**標點符號:中文句用全形、英文句用半形(以易讀為準,不機械套用)**:
- **中文句子用中文(全形)標點**:,。、;:「」『』()!?——…… 例:「測試策略要先問:哪裡最怕壞掉?」
- **英文句子用英文(半形)標點**:, . ; : " " ( ) ! ? 例:`Shift left, shift right.`
- **中英混排**:以句子的主要語言為準——整句是中文就用全形標點,句中夾的英文詞照英文原樣拼寫(不改字),但句子的逗號句號仍用全形。
- **「視情況」的意思:以看起來易讀為準,不要機械替換**。**日期、數字、程式碼、檔名、網址、版本號、縮寫、單位、比例一律用半形**,不可全形化:
  - 日期寫 `7/12`,**不是** `7／12`;`2026-07-12`,不是 `2026－07－12`
  - 數字 / 比例 / 尺寸:`16:9`、`0.5 吋`、`fontScale="80000"`
  - 程式碼、XML、檔名、路徑、網址一律照原樣半形(`buAutoNum`、`OUTLINE.md`、`https://…`)
- 判準:**替換後更難讀 / 破壞技術意義,就不要換**;純中文敘述句才是全形標點的主場。
- 例外:**參考資料與引用原文照原樣呈現**,不動標點。

**「三方視角」是每個主題的必備欄位**:每個主題都要有一個「三方視角」區塊,底下三行,分別用 `[PM]`、`[QA]`、`[RD]` 標籤開頭,各寫**該角色對這個主題的具體角度 / 例子**(一行一個角色,具體、不抽象)。再加一句 **「簡單來說:…」** 的白話總結收尾(同一風格,建議比照)。這三行剛好就是 3.B 要求的三方例子的落地呈現。**若一個主題跨多頁,三方視角只放在該主題最後一頁、續頁標題加 (cont'd)**(見第 8 步)。渲染規格見第 8 步「三方視角欄位」。

**OUTLINE.md 標記圖檔 → 該圖獨立一頁**:若 `OUTLINE.md` 的內容裡**標記了圖檔的檔名**(例如出現 `xxx.png`、`xxx.jpg`、`.jpeg`、`.gif`、`.svg` 等圖檔名),表示**要產一頁專門放那張圖**。做法:
- **找圖**:依檔名在 `20_References/` 裡找該圖檔(找不到再找 `10_Input/`、`90_Excluded/`);**檔名比對不分大小寫**。找不到就在該頁標示「缺圖:<檔名>」並**回報使用者**,不要硬編或亂找一張頂替。
- **擺圖**:圖**置中、等比例縮放**塞進內容區(**不變形、不超出畫面、不蓋到標題**——比照第 8 步版面安全);圖太大就縮進內容區,太小別硬放大到模糊。
- **位置**:這一頁放在 `OUTLINE.md` 標記它的那個位置(章節順序不變)。可加簡短標題 / 圖說(取自 OUTLINE 該處文字)。
- **頁尾照常**:進度條等頁尾元素照放;圖檔來源資料夾若有 `URL.txt`(不分大小寫),在頁面標來源。
- 圖片頁若剛好是某主題的最後一頁、又需要放三方視角,**圖與面板不可重疊**——留不下就把三方視角放到該主題另起的最後一頁(見「多頁主題」)。

**英文技術縮寫規則**:縮寫**不在內文展開全名**(保持內文乾淨);改成**每一張用到該縮寫的投影片,都在角落用極小字列出「縮寫 = 全名」**(例如 `API = Application Programming Interface`)。同一張頁出現多個縮寫就列多行;同一縮寫每次出現的那幾頁都要附(不是只附一次)。渲染規格見第 8 步「縮寫全名角落註」。因為每頁都查得到全名,PM 可接受內文直接用縮寫。

**詞彙對照表(`GLOSSARY.md`,若專案有就以它為準)**:專案根目錄若有 `GLOSSARY.md`(中英詞彙對照表),它是**全簡報用語的權威**,製作投影片時**一律以它為準**:
- **同一個概念,整份用同一個詞**:不要一下「測試案例」一下「測項」、一下「Test Case」;凡表中有列的詞,就用表中那個中文(及對應英文),跨章一致。
- **中英對照沿用表定寫法**:需要並列時照表(通常中文為主、英文括號或並列);縮寫的「全名」也**以 `GLOSSARY.md` 為準**(縮寫角落註直接取表中的中英)。
- **表沒收錄的詞**:自行從內容決定,但同一份簡報內要一致;可順手把新出現、之後會重複的關鍵詞回填 `GLOSSARY.md`(告知使用者)。
- **衝突處理**:素材或 OUTLINE 內文用詞與 `GLOSSARY.md` 不同時,**以 `GLOSSARY.md` 為準**替換(引用原文 / 參考資料章節除外,原文照登)。
- 沒有 `GLOSSARY.md` 就沿用既有規則(用語自行保持一致即可)。

**「參考資料」章節是特例(放最後、不審查、原文照登)**:`OUTLINE.md` 最後若有一個「**參考資料 / 參考文獻 / References**」章節(出處清單),它是**後置資料**,特殊處理:
- **不經完整小組審查**——它不是要講給受眾理解的內容,是出處清單,跳過第 6 步審查(在 `review-report.md` 註明此章免審)。
- **不需要三方視角、不需要 3+ 例子**。
- **原文照登**:把參考資料**逐字列出**,不改寫、不摘要、不翻譯、不精簡,保留原始出處字串(書名 / 連結 / 作者 / 年份等)方便查證。
- 用清單呈現即可,放在簡報**最後**;比照結構頁,進度條 / 縮寫角落註等可省略,**且不列入底部進度軸的章節分段**(進度軸的 N 不含參考資料)。項目多到一頁放不下就分頁(續頁標題加 `(cont'd)`)。
- 備忘稿可留白或註明「參考資料頁」。

### 6. 逐章審查(完整小組,全員通過)

每寫完一章,用完整小組各成員的視角各審一次。建議用多個 subagent 各帶一個角色設定獨立審查 —— 因為剛寫完,容易「看到自以為有寫清楚的」,而不是「實際在頁面上的」,換別人的眼睛才抓得到盲點。**例外:最後的「參考資料」章節免審(原文照登,見第 5 步)。**

審查 prompt 範本:

```
扮演 <審查者:人物誌代號:背景一句話>。
這個身分會的:<...>。不會、看了會卡住的:<...>。
請只用這個身分讀以下章節,並回答:
1. 有沒有出現看不懂、卻沒被解釋的術語或假設?逐個列出。
2. 這章有沒有至少一個貼近自身工作、看得懂的例子?
3. 哪幾頁會跟不上、需要重看?

<章節內容>
```

**通過條件**:完整小組(PM / QA / RD + USER-zen + USER-picky)**全員都過才算過**。USER-zen、USER-picky 通常會逼出「要更白話、把情境講更死、補更多直觀例子」。各方有衝突時(例如 RD 想要更多技術細節、終端用戶想更白話),以「**全員都能跟上**」為準:白話講主線、技術細節放補充或備忘。

### 7. 修到通過

針對審查抓到的問題改章節(補白話、換更貼近的例子、拆頁),然後**重審受影響的部分**。每章在 `30_Output/review-report.md` 記錄:各審查者第幾輪通過、用了哪些例子。修到全章節**完整小組全員通過**。

### 8. 產出 pptx(無巨集 + 進度條)

所有章節通過後,才把內容做成投影片,**輸出到 `30_Output/`**。產出時套用前面「開工前必問」拿到的設定:

- **先封存舊版(不覆蓋)**:產新版**之前**,先檢查 `30_Output/` 有沒有同名的既有簡報;有的話,把它**加時間戳後移到 `90_Excluded/`**(例如 `<簡報名>_YYYYMMDD-HHMMSS.pptx`),**並把 `40_Scripts/` 裡產生它的那份舊腳本一起加同一個時間戳移過去**(簡報與腳本**成組封存**,日後可回溯上一版怎麼產的),在 `90_Excluded/EXCLUDED.md` 的「封存的舊版產出」區記一筆(註明簡報 + 對應腳本)。這樣每次重跑都會留下舊版、不會被覆蓋(含使用者手動修改的版本)。用移動不要用刪除。(`review-report.md` 會在這次重跑時一併更新,不必封存。)
- **產生腳本歸位 `40_Scripts/`**:這一輪寫的 `generate_*.py` / `slides_*.py` 等產生腳本,產出完成後放到 `40_Scripts/`,**只留最近一輪**(上一輪的已在封存時移走);腳本的版本標記要對得上 `30_Output/` 的簡報。`30_Output/` 不留腳本。
- **簡報版本號 `vXX`(一個號串起三處)**:簡報有自己的版本號 `vXX`(與 skill 版本無關),**跟 `40_Scripts/generate_vXX.py` 的號同步**:同一個 `vXX` 同時是 `40_Scripts/generate_vXX.py`、`30_Output/<簡報名>_vXX.pptx`、以及(若走外部審查)`51_Revised/vXX_YYYYMMDD/`。**簡報檔名要帶 `_vXX`**(若舊檔沒帶,這版起補上);每據外部審查或重大修訂產一版就 +1(接續現有 generate 腳本的號,例如 `generate_v39.py` 之後就是 `v40`)。
- **依「沿用 / 延伸」選擇決定怎麼做**(見開工前必問第 3 點):
  - **延伸 (A)**:以剛封存的那份(即使用者可能手動改過的上一版)為**基礎底稿**,走 `pptx` skill 的 `editing.md` 在其上編輯,**保留既有頁面與手動修改**,只套用這次的變更;若 `20_References/` 有新素材,一併納入。新增 / 改動的章節仍要過受眾審查與 3 例規則。
  - **重新產生 (B)**:從 `20_References/` 重建(下列其餘規則照走)。

- **產出一份簡報**:做出一份 `<簡報名>.pptx`(完整小組審過的版本),套用下列設定(母片、長寬比、無巨集、進度條、來源網址、三方視角欄位)。封存、`OUTLINE.used.md` 快照、QA 都對這份做。
- **目錄頁(封面之後放一頁)**:封面之後、進入內容之前,固定放一頁**「目錄」**:
  - 標題「目錄」(或「課程大綱 / Agenda」),其下**依 `OUTLINE.md` 的章節順序列出每一章標題**;章名要跟內文各章、底部進度條的章節一致。**編號一律用自動編號清單(`buAutoNum`),不可手打「1. 2. 3.」**(見「編號標記 → 自動編號清單」硬規則)。
  - 這是**結構頁**,比照封面 / 章節分隔頁:**不放進度條、來源網址、三方視角面板**。
  - 樣式套母片;章很多排不下就分兩頁(第 2 頁標題加 (cont'd))。
  - 章節有增刪時,目錄要跟著更新(以 `OUTLINE.md` 為準)。
- **心智圖頁(目錄頁之後放一頁)**:目錄頁的**下一頁**,固定放一張「**簡報脈絡心智圖**」,把整份簡報的脈絡視覺化,讓受眾一眼看懂全貌與章節間的關係:
  - **結構**:中央節點 = 簡報主題(簡報名或課程名);第一層分支 = **各章**(依 `OUTLINE.md` 章節,順序與目錄一致;**「參考資料」章節不畫進心智圖**——它是出處清單、不是內容脈絡);每章再長出 2–4 個**第二層分支** = 該章的關鍵主題 / 重點(取自 OUTLINE 該章的小節或內容要點,幾個字就好,不塞長句)。
  - **畫法**:用 pptx 的形狀 + 連接線畫(橢圓 / 圓角矩形節點 + 線),**不是插圖片**;中央節點最大、第一層次之、第二層最小;各章分支可用**不同顏色**(建議跟底部進度條的章節配色一致,幫助對應);整體左右 / 放射狀展開,平衡分布。
  - **文字要短**:節點只放關鍵詞(章名可縮短),不放完整句;字級可比內文小,但要可讀。
  - **版面安全照第 8 步硬規則**:所有節點與連線**完整在畫面內**、互不重疊、不蓋標題;章多、放不下第二層時,可只畫到第一層(各章),或分兩頁(第 2 頁標題加 (cont'd))。
  - 這是**結構頁**,比照目錄頁:**不放進度條、來源網址、三方視角面板**;備忘稿註明「結構頁(心智圖),依 OUTLINE 產生」。
  - 章節有增刪時,心智圖跟著更新(以 `OUTLINE.md` 為準,與目錄同步)。
- **版面安全:不出界、不蓋標題(硬規則)**:
  - **所有形狀都必須完整落在投影片畫面內**(座標範圍 0..寬 × 0..高;16:9 = 0..13.333 × 0..7.5 吋)。**絕對不能把任何東西放到畫面外**(例如座標算錯把進度條標籤丟到 y≈183 吋)。畫面外的形狀會變成「看不見卻存在」的物件,撐長編輯檢視的捲動範圍,**害滑鼠滾輪滾不到下一頁**——實際踩過的案例:一份簡報殘留 238 個這種出界形狀(進度條標籤被算到 y≈183 吋),整份滾不動。
  - **不留任何殘留 / 垃圾形狀**:每頁只放真正要顯示的東西;算完座標要驗證在界內,別讓暫存、算錯、或被取代的形狀留在畫面外或重疊處。
  - **內文絕對不可蓋到標題(最常見的錯,務必做到)**:內文 / 內容文字框的**上緣(y)必須落在標題下緣之下**,中間留一點間距,兩者不可重疊。做法:
    - **先讀出標題的下緣**:標題佔位框的位置來自版面 / 母片(本範例母片 `title` 在 y≈0.20..0.67),所以標題下緣 ≈ 0.67 吋。
    - **內文框的 top 設在標題下緣之下**(例如 ≥ 0.8 吋,留約 0.1 吋間距);**最簡單可靠的做法是直接沿用母片的內文佔位框**(本範例母片 `body` 在 y≈0.95..6.93,本來就在標題下方)。
    - **絕對不要把內文框的 y 設成 0 / 設成投影片最頂**——這是目前一直發生的錯誤(內文框被放到 y=0,整個壓在 y=0.2 的標題上)。
    - 內文太長就靠 autofit 縮、或拆多頁,**不要靠把內文往上移到標題區來騰空間**。
  - 同理,三方視角面板不蓋內文、進度條不蓋頁碼(見各自規則)。**總原則:任兩個元素不重疊、且全部在畫面內。**
- **母片**:選了母片就用該母片做(走 `pptx` skill 的 `editing.md`);沒選或無母片就從零做(`pptxgenjs.md`)。
- **字級照投影片母片為基準 + 啟用 autofit**:這裡的「母片」一律指**投影片母片(Slide Master)/ 版面配置(Slide Layouts)**,**不是備忘稿母片**。各文字層級(標題、內文、項目等)**以投影片母片定義的字級為基準,不要手動改成別的數字**——**字偏小是母片本身的設定,不另外補償放大 / 縮小**。同時**在文字框啟用「文字溢出時自動縮小(Shrink text on overflow / autofit)」**。內容(含三方視角、簡單來說)套到對應層級就沿用該層級基準字級。例外:本 skill 自訂、疊在母片之外的極小附加註記(進度條標籤、來源網址、縮寫角落註)是獨立小字,不在此限。
- **⚠ autofit 硬規則:禁止「空的 `<a:normAutofit/>`」(內文蓋面板的頭號原因)**:空的 normAutofit 在 **PowerPoint 不會縮**(LibreOffice 會即時重算、轉圖看起來正常,反而掩蓋問題;實際踩過:一頁 12 段 + 空 normAutofit 就是這樣蓋到面板)。**每個內文框只能二選一**:
  - **(首選)內容本來就放得進框**:產出前用**行數預算**估——可容行數 ≈ 框高 ÷(母片字級 × 1.2)再打 8 折(例:3.7 吋框 + 18pt ≈ 9–10 行;子項、長句折行都算);**需要行數超過就拆頁**(見「多頁主題」)。
  - **(次選)嵌入實算的 `fontScale`** 進 `normAutofit`,讓 PowerPoint 照縮放顯示。
  - 一句話:**沒算過、又留空 normAutofit = 等著爆框蓋面板**。XML 寫法、fontScale 計算與程式化檢查見 `TECH_NOTES.md` §2。
- **內文有「編號標記」就用自動編號清單呈現(硬規則,不是建議)**:當素材 / `OUTLINE.md` 的內文是**用編號列出的**(像 `1. ` `2. ` `3. `、`1) `、`(1) `、`一、二、`,**以及圈號 `①②③…⑳`、`⑴⑵⑶` 這類 enclosed 數字**),投影片**一律**要用 **PowerPoint 的「自動編號清單」**呈現(段落套 `buAutoNum`,阿拉伯數字加句點用 `type="arabicPeriod"`)。**嚴禁把號碼(含圈號)當普通文字打進 `<a:t>` 裡**。做法:
  - **強制移除手打號碼 / 圈號**:項目文字開頭的手打號碼(`1. `、`1) `、`(1) `、`一、`)與圈號(`①`~`⑳` 等)一律**從文字刪掉**,號碼交給 `buAutoNum` 自動產生——否則重複編號或變假編號。
  - **跨頁接續連號**:同一清單跨多頁時,續頁的 `buAutoNum` 用 `startAt="N"` 接續(第一頁 1–4 → 續頁 5–8),不要每頁從 1 重來。
  - 好處:增刪項目自動重編號、縮排正確、樣式統一。無序要點改用項目符號(`buChar`);**目錄頁一律用自動編號**,不可手打。
  - `pPr` 範本、圈號 Unicode 範圍、偵測樣式與程式化檢查見 `TECH_NOTES.md` §3。
- **長寬比**:用使用者選定的比例設定投影片尺寸(16:9 = 13.333×7.5 吋、4:3 = 10×7.5 吋,或使用者指定的其他尺寸)。
- **禁止巨集**:一律存成乾淨的 `.pptx`,不帶任何 VBA / 巨集(見「安全:禁止巨集」)。
- **多頁主題(分頁 + cont'd)— 裝不下就「強制」拆頁,不要硬塞**:一個主題(topic)一頁放不下時,**必須**拆成多頁,不可把內文硬塞到爆框、也不可靠 autofit 縮到看不清。判斷標準是「**內文要能完整顯示在內文框內、且下方還留得出三方視角面板(含與內文約 0.5 吋間距)**」:
  - **內文量的上限(用「行」估,和 autofit 硬規則同一把尺)**:內文框高度有限(常見母片內文區約 3.7 吋高),上限用第 8 步「autofit 硬規則」的**可容行數公式**估(框高 ÷ 字級×1.2、打 8 折;例:3.7 吋 + 18pt ≈ **9–10 行**;子項、長句折行都算行數)。**需要行數超過可容行數就拆頁**——不要塞到 20 行、40 行(實際踩過的案例:一頁硬塞約 45 段,面板再怎麼移都會被爆出來的內文蓋住,這種一定要拆)。
  - **拆法**:把內文分到多頁,**第 2 頁起標題後面加「(cont'd)」**;每頁內文都要落在內文框內、不爆框。
  - **三方視角只放最後一頁**:同一主題的多頁中,「三方視角」面板**只放在最後一頁**(且那頁的內文也要短到留得下面板);前面的續頁不放面板、專心放內容。
  - 其餘每頁元素(進度條、來源網址、縮寫角落註)仍**每頁照放**。三方審查針對整個主題(看完所有頁)進行,並在最後一頁呈現三方視角。
  - 寧可**多一頁、每頁清爽**,也不要一頁塞爆讓面板蓋住內文。
  - **拆頁 / 加頁的技術注意**:在既有 pptx 上新增投影片要同步更新四處(slide xml + rels、Content_Types、presentation rels、sldIdLst),**且新 `rId` 絕不可與既有撞號**(撞號會蓋掉 theme / notesMaster,整份壞掉);整份重產(pptxgenjs)無此問題、首選重產。四處清單、rId 發號規則與壞檔徵兆見 `TECH_NOTES.md` §1。
- **三方視角欄位(每個主題必備;多頁主題放在最後一頁)**:主內容下方,固定放一個「三方視角」區塊。**呈現樣式(沿用使用者偏好的版本)**:
  - **底層放一個淺色圓角面板**(圓角矩形 roundRect),把標題 + 三行 + 簡單來說都框在裡面;面板填很淡的底色(預設淺藍灰 `EDF2F7`,或改用母片配色的淺色調),無邊框或極淡邊框。
  - **小標題「三方視角」**:用**第 5 個顏色**——必須**和下面四種顏色(PM 藍、QA 綠、RD 紫、簡單來說 灰)都不同**(預設用深橙 `C2410C`,或母片色系中一個與那四色明顯區隔的強調色);**粗體**;**字級比下面三行大 4 pt**(例如三行 10pt 時,標題 14pt)。
  - 其下**三行,每行約 10pt,各以 `[PM]` / `[QA]` / `[RD]` 開頭,整行用該角色的專屬色**:`[PM]` → 藍 `005B96`、`[QA]` → 綠 `007A5E`、`[RD]` → 紫 `6B4C9A`(沒有母片特定配色時用這組;有的話可換成母片色系裡三個夠區隔的顏色)。每行寫該角色對本主題的具體角度 / 例子,一行一個角色。
  - 最後一行 **「簡單來說:…」**:灰色 `555555`、**斜體**,白話總結。
  - **面板內所有文字(含「三方視角」標題、三行、簡單來說)一律靠左對齊**,標題不要置中。
  - 三個標籤與順序固定(PM→QA→RD),跨頁一致;這是必備欄位,缺了視同未完成。
  - **位置 — 絕對不能蓋住內文,且要留「實際間距」**:面板是**獨立色塊**,會遮住底下的東西,所以**上緣必須在內文文字結束之後**,而且要留**實際看得出來的間距(不能只差一點點)**——實際踩過的案例:面板上緣 4.75 吋、內文框底 4.65 吋只差 0.1 吋,內文一溢出就被蓋到;修正案例把面板**往下移到上緣 ≈ 5.43 吋**(與內文約留 0.7–0.8 吋),就不蓋了。做法:把面板放到下半部、與內文之間留約 **0.5 吋以上**的空白;**注意「蓋到」看的是內文『實際文字』有沒有溢出框、撞到面板,不是只看內文框的標示高度**——內文太多、autofit 縮了還是爆框時,**不要只把面板往下挪(挪到最後還是會被爆出來的內文蓋住),而是要減量或拆成多頁**(見「多頁主題」)。
  - **下緣**只要留在「來源網址 / 進度條」之上即可,可以**比以前更靠下**(修正版面板底緣到 ≈ 6.73 吋、仍在進度條 6.85 之上);亦即面板被夾在「內文之下(留實際間距)、來源網址/進度條之上」的帶狀區,**優先確保不蓋內文**。
  - 封面 / 章節分隔頁可省略。
- **進度條 / 小大綱(每頁底部)**:每張**內容頁**底部都要放一條進度指示,讓受眾隨時知道「現在到哪、還剩多少」。做法見下方「進度條設計」。封面 / 章節分隔頁可省略。
- **縮寫全名角落註**:每張用到英文技術縮寫的投影片,在**角落**用極小字列出「縮寫 = 全名」。要求:
  - **字級比簡報內文最小字級再略小一級**(會是全頁最小的字;若內文最小是 N pt,這裡用比 N 再小一點、仍勉強可辨識的尺寸,例如同來源網址那一級或更小)。
  - **不影響排版**:用絕對定位浮在角落,不擠壓 / 不推動內文。
  - 放在**不與進度條、來源網址、頁尾、頁碼衝突**的角落(例如右下或左下角的剩餘空間);跨頁位置一致。
  - 一頁多個縮寫就列多行(極小字);長寬比改變時座標跟著換算。

#### 進度條設計

把它做成**有意義的小大綱**,不是裝飾色條(`pptx` skill 禁止橫貫整頁的裝飾條,但帶資訊的進度 / 導覽元件是合理例外,前提是要做得像導覽、不像填充)。

- **形式**:沿頁底排成 N 個分段(N = 章節數),一段對一章,段與段之間留小間隙(分段式,而非單一實心條)。**「參考資料」章節不列入進度軸**——它是後置附錄、不是課程進度,所以 N 不含它,參考資料頁本身也不放進度條(比照結構頁)。
- **狀態用顏色區分**:已上過的章節用「完成色」、目前章節用「主色高亮」、還沒上的用淺灰。
- **加文字定位**:每段下方放極小的章節短標題(這就是「小大綱」),目前章節的標題加深;另可在角落放「2 / N」的頁章標示。
- **位置**:靠頁底,但**不可蓋到頁碼**——往下移、貼近底緣(例如 16:9 在 y ≈ 6.85 吋),並把**頁碼擺在進度條沒占用的角落**(例如最底的左下或右下角,與進度條錯開)。高度約 0.12–0.18 吋,左右對齊內容邊界(約 0.6 吋邊距)。
- **一致**:每頁同一位置、同一套配色,跨頁不要跳動。
- **長寬比要跟著變**:座標依實際投影片尺寸換算(4:3、自訂尺寸時不要沿用 16:9 的 y 值)。
- **章節很多時**(例如 > 8 章):分段太細,改用「目前章節高亮的一行小標題列 + 一條細進度比例」或只顯示鄰近章節。

#### 來源網址(進度條上方)

若某頁的內容來自帶 `URL.txt` 的資料夾,**或來自第 2 步的上網查資料**,在**進度條正上方**放一行**來源網址**:

- **位置**:進度條正上方一點點(16:9 約 y ≈ 6.6 吋),不要蓋到進度條或內文。
- **字很小就好**:用很小的字級(例如 7–8 pt)、低調的灰色。目的只是**讓人知道這裡有來源**,不必醒目;網址太長可截斷或只顯示網域(例如 `來源:example.com/…`)。
- 沒有任何來源(既無 `URL.txt`、也非上網查來)的頁面就不放。一頁若用到多個來源(資料夾或網路),擇主要來源放一行即可(或以 `; ` 串接,仍維持一行、極小)。
- 長寬比改變時 y 座標跟著換算,維持在進度條正上方。

> **頁尾元素由下而上、互不重疊的順序**(16:9 參考值):頁碼 / 進度條(最底,y ≈ 6.85,兩者錯開不互蓋)→ 來源網址(y ≈ 6.6)→ 三方視角面板(底緣可到 ≈ 6.7、只要在來源網址/進度條之上;上緣與內文留約 0.5 吋以上實際間距)→ 內文。其他長寬比依比例換算,維持此上下順序與不重疊。

#### 備忘稿(speaker notes):講者備忘 + 本頁資料來源

**每一頁的備忘稿包含三部分**:① 一段簡短的**講者備忘**(這頁要講的重點 / 串場,方便實際授課);② 一行**本頁資料來源**,讓使用者日後順著它回頭找到來源、進行手動修改;③ **本頁「三方視角」的副本**(有三方視角面板的頁才放)。

- **來源寫什麼**:本頁內容所根據的——① 用到的 `20_References/` 素材(**用採用後的資料夾名,例如 `066_迴歸策略`**;細到檔名更好);② 對應的 `OUTLINE.md` 章節 / 段落;③ 若有上網查、或該資料夾有 `URL.txt`(不分大小寫),附**來源網址**。
- **格式**:沿用現有產出的「`素材:…`」寫法、再把章節 / 網址接上即可(一頁可多筆),例如:
  `素材:066_迴歸策略;OUTLINE 第4章「迴歸測試策略」;來源:https://example.com/...`
  圖檔頁就寫該圖檔名 / 來源;純結構頁(封面、目錄、章節分隔)可寫「結構頁,無素材來源」或留白。
- **三方視角副本**:凡是**有三方視角面板的頁**(即某主題的最後一頁),把該面板的內容**原封不動複製一份到備忘稿**——含「三方視角」標題、`[PM]` / `[QA]` / `[RD]` 三行、以及「簡單來說:…」那行(文字與投影片上一致)。目的:講者看備忘稿就能照著講三方觀點,不必抬頭看投影片。沒有三方視角面板的頁(續頁 / 結構頁)就不放這一段。
- **三段的順序**:講者備忘 → 素材來源 → 三方視角副本(照這個順序,方便掃讀)。
- **只放在備忘稿、不顯示在投影片上**——這是給作者授課 + 追溯用的(投影片正面的「來源網址」是另一條規則,給觀眾看的極小字)。三方視角在投影片正面仍照常呈現;備忘稿只是**多放一份副本**,不是搬走。
- **技術注意**:加備忘稿要建 `notesSlide` 並補齊關係與 Content Types,`rId` 一樣不可撞號——細節見 `TECH_NOTES.md` §4。

排版、配色、字級、視覺品質一律遵循 `pptx` skill,這份 skill 不重複那些規則。

### 8.5 據外部審查修訂(僅當 `50_Reviewer/` 有意見時)

這是**產出之後**的外部回饋迴圈,和產出前的內部完整小組並存。`50_Reviewer/` 是空的就跳過整步。有意見時:

1. **讀意見**:把 `50_Reviewer/` 裡的意見**整批**逐檔 / 逐則讀完,能對應到哪一頁就記下對應頁。

2. **先把每一條意見分四類(關鍵:不是照單全收、也不是全丟回使用者)**:
   - **A 類 — 客觀修正**:事實 / 數字 / 計算錯誤、失效或不當來源、錯字、明顯的版面 / 一致性問題。→ **可自動採納**修改。
   - **B 類 — 編輯 / 策略 / 範圍**:砍頁數、重排章節、改教學節奏、增減演練 / 互動、把清單改成圖、金句獨立成頁等**主觀取捨**。→ **不自動改**;整理成「建議修訂計畫」給使用者,**核准後才動手**。
   - **C 類 — 與使用者既定規則 / 設計偏好衝突**:例如意見要拿掉三方視角、改參考資料呈現、違反「不用『你』」等**SKILL 裡明訂的規則或使用者偏好**。→ **預設不改**;列出「哪條意見 vs 哪條規則」,交使用者決定要不要**改規則**(改 SKILL 是使用者的權力,不是外部 AI 的)。
   - **D 類 — 需要使用者提供素材**:真實 prompt 截圖、公司範本 / Bug 欄位、內部案例等 AI 生不出來的東西。→ **標記待補**,不自行編造。

3. **依類別處理**:
   - **只有 A 類可以自動改進投影片**;B/C/D 類**不動投影片**,先產計畫 / 標記。
   - **改完務必重跑第 9 步的整套 QA**(版面安全 / 空 normAutofit / 編號清單 / 三方視角 / 標點 / 法律風險…),避免「修了 A、撞壞 B」。

4. **核准關卡(擋大改)**:
   - **安全 / 法律 / 版面硬規則不被凌駕**:凡意見與 SKILL 硬規則衝突(把號碼打進文字、內文蓋標題、有法律風險內容…)**一律不採納**,記進 LOG,不因「AI 這樣建議」就照做。
   - **大規模改動一律先核准**:凡「大改」——例如刪掉超過一定比例的頁(建議 >15%)、移除任何既有功能(三方視角 / 目錄 / 心智圖 / 進度條 / 參考資料章節等)、重排章節結構——**必須先提「建議修訂計畫」給使用者、經明確同意才做**,**不准 AI 靜默地把簡報大改或掏空**。
   - **建議修訂計畫**寫成一份清單:每條 B/C 意見 → 建議動作 → 影響(哪些頁 / 影響幾頁 / 牽動哪條規則),讓使用者逐條決定「採納 / 不採納 / 改規則」。未決的先記 LOG「待使用者決定」。

5. **產出新版(只有實際動了投影片才 +1 版)**:這批修訂完成後,簡報版本號 **+1**(接 `generate_vXX` 的號,例如下一版 `v40`);更新 `40_Scripts/` 的 `generate_vXX.py`、`30_Output/<簡報名>_vXX.pptx`(舊版照第 8 步規則連同舊腳本成組封存到 `90_Excluded/`)。(若這批全是 B/C/D、沒有可自動改的 A 類,就先不產新版,只交出修訂計畫等使用者裁示。)

6. **歸檔到 `51_Revised/vXX_YYYYMMDD/`**:
   - 把這批**用掉的審查意見**從 `50_Reviewer/` **移**過來(移動、不複製;`50_Reviewer/` 清空等下一輪)。
   - 寫一份 **`LOG.md`**,**按四類記錄**——
     - **A 已採納**:哪一條意見 → 改了哪一頁 / 怎麼改(這區就是這版的 changelog)。
     - **B 待核准 / 使用者裁示**:哪一條 → 建議動作 → 使用者決定(採納 / 不採納)。
     - **C 與規則衝突**:哪一條 → 牴觸哪條規則 → 是否改規則。
     - **D 待補素材**:哪一條 → 需要使用者提供什麼。
     - **不採納**:哪一條 → 為什麼不改(理由;與硬規則衝突要註明是哪條規則)。
   - `LOG.md` 開頭註明對應的 `30_Output/<簡報名>_vXX.pptx`。投影片本體不放這裡。

7. **刷新根目錄文件**(第 10 步):INDEX 記到最新 `vXX` 與這次外部審查修訂。

**一句話原則**:外部意見裡「**客觀錯誤**」自動修;「**主觀取捨 / 大改 / 動到既有功能或規則**」一律先提計畫、經使用者核准再動——AI 不擅自把使用者的教材大改或掏空。

### 9. QA

- **內容 QA**:跑 `extract-text 30_Output/<簡報名>.pptx`,檢查漏字、順序、佔位字殘留。**同時掃「你」字**:內文出現「你」(且不是參考資料 / 引用原文)就改寫——省略主詞或改「我們」(見第 5 步「內文用語」)。 **同時檢查標點**:中文敘述句用全形標點、英文句用半形;但日期(`7/12`)、數字、比例(`16:9`)、程式碼、檔名、網址、版本號一律保持半形,不可被全形化(見第 5 步「標點符號」)。
- **視覺 QA**:用 subagent 看轉出的圖檢查溢出 / 重疊 / 對比(照 `pptx` skill 的視覺 QA 流程)。
- **版面安全 QA(出界 / 重疊,阻擋性)**:逐頁掃描每個形狀的座標——
  - **出界檢查**:任何形狀只要有一部分落在畫面外(x / y < 0 或超過投影片寬高)就是錯,**移回畫面內或刪除**;程式化掃描方法(EMU 換算、判定式)見 `TECH_NOTES.md` §5。
  - **重疊檢查**:確認**內文沒蓋到標題**(標題下緣 ≤ 內文上緣)、三方視角面板沒蓋內文、進度條沒蓋頁碼。**用轉出的圖實際看**:內文『文字』有沒有溢出框、壓到三方視角面板(座標不重疊但文字爆框也算蓋到)。**注意:轉出的圖是 LibreOffice 算的、會自動套用 autofit 縮放,可能把「PowerPoint 裡其實會爆框」的情況藏起來**;所以除了看圖,**一定要另外做下面這個「空 normAutofit」硬檢查**。
  - **空 normAutofit 硬檢查(阻擋性,不靠轉圖)**:掃每個內文框——**只要是「空的 `<a:normAutofit/>`(沒有 `fontScale` 屬性)」就直接判未通過**,不管轉圖看起來如何(因為那在 PowerPoint 會 100% 不縮、爆框蓋面板)。修法:該頁**拆頁**,或把實算的 `fontScale` 寫進 `normAutofit`(見第 8 步 autofit 硬規則)。另外用「需要行數 vs 可容行數」複核每頁內文塞不塞得下。
  - 任一頁有出界形狀、上述重疊、內文爆框蓋到面板、或留著空的 normAutofit = 未通過,修好再交。
- **進度條 QA**:確認每張內容頁底部都有進度指示、目前章節正確高亮、跨頁位置一致且沒蓋到內文。
- **目錄頁 QA**:確認封面之後有一頁「目錄」、且列出的章節與 `OUTLINE.md` 的章節、順序一致(沒漏章、沒多章);目錄頁是結構頁,不該有進度條 / 三方視角。
- **心智圖頁 QA**:確認目錄頁之後有一頁「簡報脈絡心智圖」——中央主題 + 各章分支齊全、與 `OUTLINE.md` 章節一致(沒漏章;**「參考資料」章節除外,不應出現在心智圖**);節點與連線全部在畫面內、互不重疊、不蓋標題;是結構頁(無進度條 / 三方視角)。
- **編號清單 QA(阻擋性,掃 XML)**:掃每個內文 / 目錄段落的 `<a:t>` 文字——**只要有段落文字開頭還留著手打號碼或圈號**(符合 `^\s*\d+[.、)]\s*`、`一、`、`(1)`,**或開頭是 `①`~`⑳` 這類圈號字元**)就判**未通過**,代表號碼沒被移除、沒改成自動編號。正解:刪掉文字裡的號碼 / 圈號前綴 + 該段套 `buAutoNum`。反向也查:凡是**該用編號的清單**(目錄、有序步驟),段落要**確實有 `buAutoNum`**,不是純文字假編號;**跨頁的清單續頁要有 `startAt` 接續連號**(而非又從 1 開始)。
- **詞彙一致性 QA(若有 `GLOSSARY.md`)**:抽查投影片用語是否與 `GLOSSARY.md` 一致——同一概念用同一個詞、中英對照與縮寫全名照表;發現與表不符(且非引用原文)就改成表定用語。
- **備忘稿 QA**:抽查內容頁的備忘稿,確認有「講者備忘」+「本頁來源」(用到的 `20_References/` 素材 / OUTLINE 章節 / 來源網址);**有三方視角面板的頁,備忘稿要有一份三方視角副本**([PM]/[QA]/[RD] + 簡單來說,與投影片一致);結構頁(封面、目錄、章節分隔)可註明無素材來源或留白。
- **三方視角 QA**:確認**每個主題**都有「三方視角」面板(多頁主題在最後一頁)、且 `[PM]`/`[QA]`/`[RD]` 三行齊全(順序一致)並有「簡單來說」收尾;多頁主題的續頁標題有 `(cont'd)`。另外檢查版面:**面板沒有壓到任何內文**(上緣在內文之下)、下緣在來源網址/進度條之上;「三方視角」標題用**第 5 個(與四色不同)顏色、比三行大 4pt**;面板內文字**全部靠左對齊**。某主題缺欄位、或面板蓋到內文 = 未通過。
- **來源網址 QA**:用到帶 `URL.txt` 資料夾的頁面,確認進度條上方有那行極小來源網址、沒蓋到進度條或內文;沒來源的頁面則沒有多餘的那行。`(O)` 優先素材有確實被優先採用。
- **設定 QA**:確認長寬比與使用者選的一致;若有套母片,確認外觀有正確套上,而且套的是**投影片母片 / 版面配置**(`ppt/slideMasters/`、`ppt/slideLayouts/`),**不是備忘稿母片 / 講義母片**。
- **字級 QA**:若有套母片,確認各文字層級的**基準字級與投影片母片一致**(沒被手動改成別的數字,且來源是投影片母片、不是備忘稿母片),且文字框**已啟用 autofit(文字溢出時自動縮小)**。
- **安全 QA(禁止巨集)**:確認副檔名是 `.pptx`(非 `.pptm`/`.potm`),`unzip -l 30_Output/<簡報名>.pptx | grep -i vba` 為空(無 `ppt/vbaProject.bin`)。
- **法律風險 QA(阻擋性,不過就不能產出;第 5 步是寫作時預防、這裡是產出前最後一道攔截,兩者分工不重複)**:逐頁(含標題、內文、三方視角的 `[PM]`/`[QA]`/`[RD]` 例子、講者備忘)檢查有沒有**可能讓講者被告或違反公司規範**的內容,有就**改寫或刪除**,不能留。重點掃描:
  - **性騷擾 / 性暗示**:黃色笑話、帶性意味的舉例或圖、對外貌身材的評論。
  - **歧視**:針對種族、族群、性別、性傾向、年齡、宗教、身心障礙、國籍、婚育狀態等的刻板印象、貶抑或差別待遇用語(連「舉例方便」用的刻板印象也不行)。
  - **職場霸凌 / 人身攻擊**:貶低、羞辱、嘲諷特定人或群體;用真實同事 / 主管當負面例子。
  - **誹謗 / 商譽**:對真實人物或公司做未經證實的負面指控;拿競品或他人指名道姓地貶低。
  - **隱私 / 機密**:真實個資、客戶資料、未公開的內部機密 / 營業秘密。
  - **其他法律風險**:侵權重製(整段抄他人著作 / 圖)、不實宣稱、煽動違法。
  例子是最容易出事的地方(為了「貼近」而用了刻板印象或真實人名)——特別盯。若有風險內容來自使用者提供的素材而無法安全改寫,**標示出來並回報使用者**,不要默默放進去。處理結果記進 `review-report.md`。
- **外部審查 QA(若這輪有處理 `50_Reviewer/`)**:確認①每條意見都**分過四類**(A 客觀 / B 策略 / C 衝突規則 / D 待補),**只有 A 類被自動改**、B/C/D 沒有被擅自套用;②**大改(刪 >15% 頁、移除既有功能、重排結構)有先提計畫並經使用者核准**才做,沒有靜默大改 / 掏空;③改完有**重跑整套 QA**;④(若有動投影片)新版簡報 / `generate_vXX.py` / `51_Revised/vXX_YYYYMMDD/` **三處同號**、簡報檔名帶 `_vXX`;⑤`51_Revised/vXX_YYYYMMDD/` 內有**用掉的意見 + `LOG.md`**,LOG 按四類 + 不採納分區記錄;⑥`50_Reviewer/` 已清空;⑦沒有任何外部意見凌駕硬規則。
- **素材 QA**:確認 `90_Excluded/EXCLUDED.md` 有逐筆排除原因;簡報內容只來自 `20_References/`,沒用到被排除的素材。若這是重跑,確認舊版簡報**連同其產生腳本**已加時間戳成組封存到 `90_Excluded/`、`30_Output/` 沒被覆蓋掉舊版。另確認**產生腳本已歸位 `40_Scripts/` 且只留最近一輪**、`30_Output/` 沒殘留腳本。
- **驗收 QA**:打開 `30_Output/review-report.md`,確認**每一章**都有完整小組全員通過標記、且列了三個以上例子(涵蓋三方)。任一章缺項 = 未通過,回第 6/7 步。
- 最後確認 `30_Output/` 裡同時有 `.pptx` 與 `review-report.md`。

### 10. 更新根目錄文件(INDEX.md / README.md / CLAUDE.md)

收尾時,在**專案根目錄**寫 / 刷新這三份文件,反映這次跑完的當前狀態。三者用途不同,各寫各的、不要互相複製貼上;每次跑都重寫成最新,不堆疊歷史(歷史看 `review-report.md` 與 `90_Excluded/`)。

**`INDEX.md` — 當前狀態地圖(給人快速掌握全局)**,建議涵蓋:
- **主題與狀態**:簡報主題、目前產到第幾版 / 產出檔名、最後更新時間。
- **OUTLINE 指紋與 SKILL 版本**:這次所用 `OUTLINE.md` 的指紋,以及本 skill frontmatter 的 `version`(供第 0 步下次比對是否變更)。
- **素材資料夾內容清單 / 指紋**:`20_References/` 與 `90_Excluded/`(排除素材)裡各資料夾目前的檔案清單(或內容指紋),供下次比對「資料夾有沒有被擴充 / 改動」(見第 1 步)。
- **設定**:套用的母片、長寬比。
- **章節地圖**:每章標題 → 審查小組(成員各是誰)→ 主要取自哪些素材(若該素材有 `URL.txt` 或上網查來的來源也標上)。
- **素材一覽**:`20_References/` 現有素材清單(標出 `(O)` 優先項);`10_Input/` 還有沒有待篩;`40_Scripts/` 目前這一輪的腳本;`50_Reviewer/` 有沒有待處理的外部意見;`51_Revised/` 歷次據外部審查的修訂版(vXX_YYYYMMDD);`90_Excluded/` 排除了哪些 + 封存了哪些舊版(簡報 + 腳本);這次有沒有上網補資料、用了哪些網址。
- **產出位置**:`30_Output/` 下的檔案。

另外**存一份 `OUTLINE.md` 快照到 `30_Output/OUTLINE.used.md`**(覆蓋成這次用的版本),供第 0 步下次 diff 出「哪幾章變了」。

格式範例(精簡即可):

```
# 專案總覽:<主題>
更新時間:<YYYY-MM-DD HH:MM> · 目前版本:<簡報名>.pptx · 母片:<x> · 長寬比:16:9
OUTLINE 指紋:<hash 前 12 碼> · SKILL 版本:<version> · 審查小組:<列出成員代號>
素材資料夾指紋:20_References=<各資料夾:檔數/指紋> · 90_Excluded(排除素材)=<各資料夾:檔數/指紋>

## 章節地圖
1. <標題> — 完整小組通過 — 取自 <素材>(來源:<URL 若有>)
2. ...

## 素材一覽
- 20_References:<清單,標 (O)>
- 10_Input:<待篩數 / 已清空>
- 90_Excluded:排除 <n> 筆、封存舊版 <n> 份
- 上網補資料:<無 / 用了哪些網址>

## 產出
- 30_Output/<簡報名>.pptx、review-report.md、OUTLINE.used.md
```

**`README.md` — 給人看的專案說明**:這個專案是什麼主題、資料夾各區放什麼、素材該放哪(`20_References/` 或 `10_Input/`)、設定檔是根目錄的 `AUDIENCE.md` / `OUTLINE.md`、怎麼用 ref2pptx 重跑、產出在 `30_Output/`。大致穩定,主要確保存在且結構描述正確;有「目前版本」之類的狀態行就順手更新。

**`CLAUDE.md` — 給 Claude Code 的專案記憶**:讓未來在這個資料夾開的 Claude session 一讀就懂該怎麼接手。**保持精簡,並且「不要把產出規則抄進來」**——所有製作規則(母片 / 進度條 / 三方視角 / 編號清單 / 版面安全…)一律**以 `SKILL.md` 為準**,CLAUDE.md 只需**指向 skill** 並記**本專案特定的事**,否則規則放兩地、一改就脫節(這是 CLAUDE.md 最容易過期的原因)。建議只寫:① 這是一個 ref2pptx 專案、**一切依 `ref2pptx` skill 處理(規則看 skill,不在此重述)**;② 本專案主題;③ 控制檔是根目錄的 `AUDIENCE.md` / `OUTLINE.md`(有 `GLOSSARY.md` 就一併)、素材只在 `20_References/`(別去根目錄找);④ **本專案的特定決定**(例如選用的母片、長寬比、這個主題特有的用語或注意事項);⑤ **目前狀態**(產到第幾版、最後更新時間、對應的 skill 版本)。每次跑完重寫成最新。

這三份都放根目錄、都不是素材;參考素材仍只放 `20_References/`。

---

## 長大綱(分批)模式

`OUTLINE.md` 很長(預估產出超過約 40 頁,或章 / 主題數量多到一輪做不完)時,**不要嘗試一口氣產完整份**——輸出長度有限,一次全產一定會中途被截斷、或後半開始摘要 / 跳過。改走**分批迴圈**:

- **先建進度帳 `30_Output/PROGRESS.md`**:開工第一步,把 `OUTLINE.md` 的**所有章節編號列成清單**,每章一列、狀態欄(待做 / 已產 / 已審 / 已 QA)。這份清單是完整性的靠山,之後每輪更新。
- **一次只做一章**:每輪固定流程——產出該章所有投影片(含三方視角、必要時拆頁)→ 完整小組審查 → QA → 存檔 → 更新 `PROGRESS.md` → **停下回報**(本章幾頁、對應大綱幾個主題、缺圖 / 待補事項),不擅自往下一章。某章特別大就再切到「一次一節」。
- **嚴禁摘要式產出**:不准用「以此類推」「(略)」「其餘章節同上」帶過;每個主題都要有實際內文,塞不下就拆頁,不是省略。
- **每章各產一個分章 pptx、最後合併**:長簡報不要邊生邊往同一個大檔塞頁(加頁的 rId 風險會累積、出錯難回溯);分章檔各自過審與 QA,全部完成後照 `TECH_NOTES.md` §1 的規則合併(或用 pptxgenjs 整份重組)。合併後的完整檔才是 `<簡報名>.pptx`。
- **完整性對帳(最後一關)**:全部章節做完後,拿 `PROGRESS.md` 對三樣東西——① 每章狀態都是「已 QA」;② 合併檔的章 / 頁與 `OUTLINE.md` 章節一一對應(沒漏章);③ 目錄頁、心智圖、進度條的章節數一致。對不上就補完才交付。
- **中斷可續跑**:每輪都把進度寫進 `PROGRESS.md` 與 `INDEX.md`(含 OUTLINE 指紋),中斷後下一輪讀進度帳就知道從哪一章接續,不必整份重來。
- **建議先跑 1–2 章當樣板**:確認母片、風格、三方視角、編號清單都符合期待,再以同一套設定跑完其餘章節,品質才一致。

## 驗收標準對照(交付前自我檢查)

| 條件 | 要求 | 證據在哪 |
|------|------|----------|
| 素材篩選 | 10_Input 已篩,不適用的移到 90_Excluded 並記原因 | `90_Excluded/EXCLUDED.md` + report 摘要 |
| 3.A | 每章都由完整小組(PM/QA/RD + USER-zen + USER-picky)審過並通過 | `30_Output/review-report.md` 逐章通過標記 |
| 3.B | 每個章節有三個以上例子,涵蓋三方視角(各方≥1) | `30_Output/review-report.md` 逐章列出的例子 |
| OUTLINE 檢查 | 每次執行有比對 OUTLINE.md 是否變更 | 第 0 步結果 + INDEX 指紋 |
| 上網補資料 | 用到的網路內容都在頁面留了來源網址 | 來源網址 QA + INDEX 紀錄 |
| 母片 | `00_Masters/` 有檔時已問過使用者並照其選擇套用 | 對話中的詢問 + 產出外觀 |
| 字級照母片 | 文字以投影片母片字級為基準、未手動改數字,且文字框啟用 autofit | 字級 QA |
| 長寬比 | 已問過使用者並套用其選的比例 | 對話中的詢問 + 設定 QA |
| 禁止巨集 | 產出是無巨集 `.pptx`,無 `vbaProject.bin` | 安全 QA(`unzip -l` 無 vba) |
| 法律風險 | 無性騷擾 / 歧視 / 職場霸凌 / 誹謗 / 隱私洩漏等可能讓講者被告的內容 | 法律風險 QA |
| 進度條 | 每張內容頁底部有進度 / 小大綱 | 進度條 QA |
| 目錄頁 | 封面後有一頁目錄,章節與 OUTLINE 一致 | 目錄頁 QA |
| 心智圖頁 | 目錄後有一頁脈絡心智圖,章節分支與 OUTLINE 一致(參考資料除外)、全在畫面內不重疊 | 心智圖頁 QA |
| 編號清單 | 編號內容(含目錄)用 buAutoNum 自動編號,文字裡沒殘留手打號碼 | 編號清單 QA |
| 圖檔頁 | OUTLINE 標記的每個圖檔都有專屬一頁、圖等比置中不變形不出界(缺圖有標示並回報) | 視覺 QA + 版面安全 QA |
| 備忘稿內容 | 每頁備忘稿有講者備忘 + 本頁來源(素材/章節/網址);有面板的頁另附三方視角副本 | 備忘稿 QA |
| 詞彙一致性 | (若有 GLOSSARY.md)全簡報用語 / 中英對照 / 縮寫全名以對照表為準、跨章一致 | 詞彙一致性 QA |
| 標點符號 | 中文句全形、英文句半形;日期 / 數字 / 程式碼 / 檔名 / 網址維持半形 | 內容 QA |
| 腳本歸位 | 產生腳本在 `40_Scripts/`(只留最近一輪);舊腳本與舊簡報成組封存到 `90_Excluded/` | 素材 QA |
| 外部審查修訂 | (若有 50_Reviewer 意見)意見分四類、只自動改 A 類客觀項、大改先經核准;改完重跑 QA;vXX 三處同號;51_Revised/vXX_YYYYMMDD 內有意見 + 分類 LOG;意見不凌駕硬規則 | 外部審查 QA |
| 長大綱完整性 | (分批模式)PROGRESS.md 每章已 QA;章 / 頁與 OUTLINE 一一對應,沒漏章 | 完整性對帳 |
| 三方視角欄位 | 每個主題有「三方視角」+ [PM]/[QA]/[RD] 三行 + 簡單來說(多頁主題在最後一頁;續頁標題 (cont'd)) | 三方視角 QA |
| 輸出 | 一份 .pptx,視覺品質符合 pptx skill | `30_Output/<簡報名>.pptx` + 視覺 QA |
| 版面安全 | 無出界形狀、內文不蓋標題、元素互不重疊 | 版面安全 QA |
| 總覽 | 根目錄 `INDEX.md`、`README.md`、`CLAUDE.md` 已刷新 | 三份根目錄文件 |

---

## 範例

**範例 1 — 完整流程**
專案 `課程_GenAI測試/`:根目錄有 `AUDIENCE.md`,`10_Input/` 放了一堆剛蒐集的筆記與截圖,`20_References/` 已有核心規格,`00_Masters/` 有一個公司母片。
動作:先篩 `10_Input/` → 一張模糊到看不清的白板照、和一份過期版規格移到 `90_Excluded/` 並記原因,其餘移進 `20_References/` → 讀根目錄 `AUDIENCE.md` → 問使用者「套這個母片嗎 / 長寬比?」→ 用 `20_References/` 排章節、逐章寫 3 例、受眾審查 → 套母片、16:9、無巨集、加底部進度條產出 → `.pptx` 與 `review-report.md` 寫進 `30_Output/`。

**範例 2 — 沒有 AUDIENCE.md**
專案根目錄沒有 `AUDIENCE.md`。
動作:先用預設受眾庫提一組對應(哪章配哪位受眾)給使用者確認,確認後再往下;不要自己悶著頭假設。

**範例 3 — 想排除 20_References 裡的東西**
篩選時發現 `20_References/` 有一份明顯離題的舊文件。
動作:**先告知使用者**「這份看起來離題,建議排除」,得到同意再移到 `90_Excluded/`;不要逕自移走使用者刻意放的素材。

---

## 注意

- 素材篩選與受眾審查是**這份 skill 的兩個重點**,不要為了趕快產出而跳過。
- **排除 = 移到 `90_Excluded/`,絕不刪除**;每筆都要在 `EXCLUDED.md` 記原因,讓使用者能覆核。
- 例子的「貼近」比「數量」更重要;湊滿三個但全是抽象換句話說,等於沒過。
- 視覺與排版細節不在這份 skill 處理範圍,一律回到 `pptx` skill。
- **pptx XML 層的技術細節(加頁 rId、normAutofit/fontScale、buAutoNum、notesSlide、版面掃描)集中在同資料夾的 `TECH_NOTES.md`**;動 XML、程式產出或做程式化 QA 前先讀對應小節。
- 若執行環境沒有可用的 `pptx` skill(例如本機 Claude Code 未安裝文件 skill),改用通用方式(pptxgenjs / python-pptx)產出,但仍要套用本 skill 自己的規則:無巨集、指定長寬比、每頁進度條。
